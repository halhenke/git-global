extern crate clap;

// include!("src/lib.rs");
// include!("src/cli.rs");

// use std::env;

fn main() {
    println!("Too hard at present to use this build script to generate completions...");

    // let outdir = match env::var_os("OUT_DIR") {
    //     None => return,
    //     Some(outdir) => outdir,
    // };
    // let mut app = get_clap_app();
    // app.gen_completions("myapp",      // We need to specify the bin name manually
    //                     Shell::Bash,  // Then say which shell to build completions for
    //                     outdir);      // Then say where write the completions to
}
